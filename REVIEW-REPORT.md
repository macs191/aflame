# TagHub — تقرير المراجعة والتحصين

**المستودع:** `macs191/aflame`  
**فرع العمل:** `fix/security-and-data-integrity-2026-09-26`  
**النطاق:** صفحات ومكونات المشروع عدا Play وKodi. لم أفتح أو أعدل `public/play.html` أو `public/code.html`.

## الحالة باختصار

أُنجزت تغييرات المصدر وقواعد Realtime Database وسكربتات الترحيل واختبارات المحاكي على فرع منفصل. **لم تُطبّق أي كتابة على Firebase الحقيقي، ولم تُنشر تغييرات إلى Vercel أو الإنتاج، ولم أرفع تغييرات GitHub بعد.** السبب أن ترحيل قاعدة حية ونشر سياسة رفض افتراضي قد يوقفان الموقع إن لم تُراجع البيانات أولاً.

## ما أُنجز

- **Firebase Auth للإدارة:** استبدال PIN الواجهة بتسجيل بريد/كلمة مرور والتحقق من custom claim باسم `admin` قبل تحميل بيانات لوحة الإدارة. اختفاء `admin.html` أو تغيير اسمه ليسا وسيلة حماية؛ صلاحية قاعدة البيانات تأتي من Rules والـclaim.
- **حماية RTDB:** إضافة `database.rules.json` بسياسة رفض افتراضي: ملف الإدارة وبيانات الإعدادات والإعلانات الخام والاشتراكات للمشرف فقط؛ ملف المستخدم يرى صاحبه؛ لا يستطيع المستخدم تعديل VIP أو الدور أو الحظر؛ طلبات التحويل تُنشأ معلقة تحت UID المالك؛ وتُقيّد القراءة الخاصة للمشاهدة بحالة الحساب وVIP وتاريخ الانتهاء.
- **فصل الكتالوج عن روابط البث:** مسارات `videosPrivate` و`liveChannels` و`matches` تحفظ السجلات الكاملة وتُقرأ وفق الصلاحيات. مسار `publicCatalog` يعرض حقول القائمة فقط، دون روابط البث/التحميل. كما فصلت إعدادات الصفحة والإعلانات المعروضة (`publicSettings` و`publicAds`) عن إعدادات الإدارة والإعلانات الخام.
- **ترحيل آمن قابل للمراجعة:** `scripts/firebase-admin/migrate-database.cjs` يقرأ ويلخص فقط افتراضياً؛ ولا يكتب إلا مع `--apply` ومتغير تأكيد للمشروع. ينقل `videos` القديمة، وينشئ إسقاطات عامة مسموحة، ويحوّل طلبات VIP إلى المسار المقيد وينقي الحقول. **لم أشغّله على قاعدة حية.**
- **منح صلاحية الإدارة:** إضافة `scripts/firebase-admin/grant-admin.cjs` لاستخدام Firebase Admin SDK لمنح/سحب custom claim دون وضع مفتاح الخدمة في المستودع.
- **الدفع اليدوي:** إزالة الاعتماد على Paymob وإضافة حقول المحفظة/PayPal/البنك واسم المزود/المحفظة ورقم العملية وهاتف/بريد المُرسل والملاحظة. يحفظ الطلب كـpending للمراجعة اليدوية فقط. لا يوجد تحصيل آلي أو تحقق آلي من وصول الأموال أو ربط Visa؛ ولا تُطلب بيانات بطاقة أو PIN أو OTP.
- **إصلاحات المراجعة السابقة:** معالجة حقن HTML وروابط البروتوكولات غير الآمنة، حماية صور/إعلانات العرض، حدود تحقق للمدخلات والاستيراد وM3U، تثبيت مسارات VIP القابلة للاستئناف، تصحيح سلوك بيانات الحساب/الحذف، وتحسين قيود التصدير والواجهة. أضيفت رؤوس حماية أساسية إلى `vercel.json`.

## التحقق

- **Firebase Emulator:** 9 اختبارات ناجحة تشمل القراءة العامة والخاصة، المشاهدة المجانية وVIP، انتهاء الاشتراك والحظر، إنشاء الحساب، طلب التحويل، حقول مراجعة الإدارة، الملكية، ومنع تصعيد صلاحية المستخدم.
- `node --check` لكل JavaScript المعدّل وسكربتات الترحيل: نجح.
- تحليل JSON لـRules وFirebase وVercel وملفات الأدوات و`git diff --check`: نجح.
- `npm audit` على أدوات الترحيل/الاختبار المحلية: بقيت تنبيهات **متوسطة فقط** (5 في Firebase CLI test tool و2 في Firebase Admin SDK؛ لا عالية/حرجة). هذه حزم أدوات منفصلة وليست مكتبات واجهة الموقع.
- معاينة المتصفح: شاشة دخول الإدارة مغلقة قبل تسجيل الدخول؛ لم تُحمّل مستمعات البيانات الخاصة. اختُبر تعقيم إعلان خبيث محلياً؛ أزيل `script` و`onerror` و`javascript:`.
- استجابات المعاينة المحلية للصفحة العامة وصفحة الإدارة والملفات الثابتة: HTTP 200. لا تمثل هذه المعاينة اتصالاً أو نشراً حياً.
- صفحتا Play وKodi لم تتغيرا.

## قبل تشغيل الإنتاج — خطوات لازمة

1. راجع نسخة احتياطية من RTDB وحدد حساب الإدارة الذي تملكه. فعّل Email/Password في Firebase Auth، وأنشئ الحساب، ثم امنحه `admin: true` بسكربت Admin SDK من بيئة خاصة خارج المستودع.
2. ضع مفتاح Service Account في مسار خاص خارج المشروع؛ **لا ترفعه إلى GitHub ولا إلى `public/` ولا إلى متغير `VITE_`**. إعداد Firebase Web `apiKey` الموجود في العميل معرّف عام، وليس كلمة مرور قاعدة البيانات. لا يلزم نقله إلى Vercel لإخفائه.
3. نفّذ `migrate-database.cjs` دون `--apply`، وراجع الملخص والنسخة الاحتياطية. بعد ذلك فقط، ومع قبولك تغيّر بنية البيانات، نفّذ الترحيل مع متغير التأكيد المذكور في `FIREBASE-VERCEL-SETUP.md`.
4. أعد تشغيل `npm test`، ثم انشر قواعد RTDB إلى `voip17` من حسابك بعد التأكد من الكتالوج وسجلات المستخدمين ووجود حساب المدير.
5. انشر ملفات الموقع على Vercel واختبر حساباً عادياً ومديراً، ومسارات free/VIP وطلبات التحويل قبل اعتمادها.
6. أدخل بنفسك وجهة الاستلام والتعليمات في إعدادات الإدارة. يجب التأكد يدوياً من التحويل في المحفظة/PayPal قبل الضغط على قبول VIP.

## تحذير توافق الصفحات المستثناة

لم تُراجع واجهتا Play وKodi حسب طلبك. قواعد البيانات الجديدة تمنع القراءة العامة لروابط البث الكاملة؛ لذلك **لا يمكن ضمان توافق هاتين الواجهتين مع سياسة RTDB الجديدة** إذا كانتا تقرآن المسارات القديمة مباشرة أو تسمحان بالمشاهدة دون تسجيل الدخول. راجع توافقهما أو اترك نشر القواعد معلقاً حتى تختبرهما—من دون تعديل الصفحتين.

## ما لا تدّعيه هذه النسخة

لا يوجد حالياً ربط حي بحساب Vercel/Firebase، ولا مفاتيح أو حسابات خدمة مقدمة لهذا التنفيذ، ولا ترحيل/نشر إنتاجي، ولا بوابة دفع أو تحقق دفع تلقائي. قواعد المحاكي ناجحة، لكن ذلك لا يعني أنها منشورة في مشروع `voip17`.
