# Firebase Auth, قواعد قاعدة البيانات وVercel

هذه الإعدادات تستهدف مشروع Firebase الموجود `voip17`، وتُبقي واجهات **Play** و**Kodi** خارج نطاق العمل. لا تتضمن الملفات أي مفتاح Service Account أو كلمة مرور.

## التصميم الأمني

- ملفات المشاهدة الكاملة موجودة في `videosPrivate` و`liveChannels` و`matches`. لا يقرأها المستخدم غير المسجّل؛ ويمنع RTDB Rules مستخدم VIP من قراءة محتوى VIP إذا انتهى اشتراكه أو كان الحساب محظوراً.
- بيانات الكتالوج العامة فقط (العنوان، التصنيف، الملصق، حالة VIP، ونحوها) موجودة في `publicCatalog`. لا تنسخ هذه الكتالوجات روابط البث أو التحميل.
- `firebaseConfig.apiKey` في الواجهة **معرّف عميل عام وليس سرّاً**. لا يمكن إخفاؤه داخل Vercel؛ الحماية الحقيقية هي Firebase Authentication وقواعد RTDB. لا تضع ملف Service Account أو مفتاح Admin SDK في `public/` أو أي متغير `VITE_` أو في Git.
- توثيق واجهة Vercel من متغيرات البيئة غير مطلوب لهذا المسار: الموقع يتصل بـ Firebase مباشرة عبر SDK وقواعد الملكية/الدور. لم نضع صلاحيات Admin SDK على المتصفح أو في Vercel.
- إخفاء اسم `admin.html` لا يحمي قاعدة البيانات. الدخول الآن بالبريد/كلمة المرور، ثم ادعاء Firebase Auth مخصص `admin: true` صادر حصراً من Admin SDK.

## إعداد لمرة واحدة قبل نشر قواعد الرفض الافتراضي

1. من Firebase Console للمشروع `voip17` فعّل **Authentication → Sign-in method → Email/Password**، وأضف حسابك الإداري من Console. لا يوجد تسجيل إداري مفتوح للعامة.
2. نزّل Service Account key من إعدادات المشروع واحفظه خارج المستودع ومجلدات الموقع، بصلاحيات ملفات خاصة. لا ترسله في المحادثة ولا ترفعه إلى GitHub أو Vercel.
3. حضّر بيئة أدوات الإدارة محلياً:

   ```bash
   cd scripts/firebase-admin
   npm install
   export GOOGLE_APPLICATION_CREDENTIALS="/مسار-خاص-خارج-المستودع/service-account.json"
   export FIREBASE_PROJECT_ID="voip17"
   ```

4. عيّن UID حسابك الإداري من صفحة Authentication. امنحه صلاحية الإدارة:

   ```bash
   node grant-admin.cjs FIREBASE_AUTH_UID
   ```

   لتراجعها لاحقاً: `node grant-admin.cjs FIREBASE_AUTH_UID --revoke`. لا تمنح الادعاء إلا لحساب تملكه. سجّل الخروج ثم ادخل من صفحة الإدارة بعد تعيينه.
5. خذ نسخة احتياطية من Realtime Database أولاً. ثم نفّذ فحص الترحيل **دون كتابة**:

   ```bash
   node migrate-database.cjs
   ```

   يفحص السكربت ملف المستخدمين، ينقل سجلات VOD القديمة من `videos` إلى `videosPrivate`، يبني نسخ `publicCatalog` الخالية من روابط التشغيل، وينقل طلبات VIP القديمة إلى `/vipRequests/{uid}/{requestId}`. لا يطبع محتوى أو بيانات شخصية.
6. بعد مراجعة الملخص والنسخة الاحتياطية، طبّق الترحيل يدوياً:

   ```bash
   CONFIRM_FIREBASE_MIGRATION=voip17 node migrate-database.cjs --apply
   ```

   لا تنشر القواعد قبل اكتمال الترحيل والتحقق من الكتالوج ووجود ملفات المستخدمين. الترحيل **يغيّر البيانات**؛ لذلك لا يُنفذ تلقائياً.
7. اختبر القواعد محلياً:

   ```bash
   cd scripts/firebase-rules-tests
   npm install
   npm test
   ```

8. تنبيه توافق: لم تُراجع صفحات Play وKodi. القواعد الجديدة تحجب روابط التشغيل الكاملة عن الضيوف؛ إذا كانت أي من هاتين الصفحتين تقرأ Firebase مباشرة أو تعتمد على المسارات القديمة، فقد تتأثر. اختبر التوافق دون تعديل الصفحتين قبل نشر القواعد.

9. عند نجاح الترحيل والاختبار وتسجيل حساب المدير، انشر القواعد إلى Firebase:

   ```bash
   cd ../..
   npx firebase-tools deploy --only database --project voip17
   ```

   عملية النشر هذه مقصودة لكنها ليست منفذة من هنا؛ قواعد غير مختبرة قد تقطع الخدمة. راجع الحالة في Firebase Console بعد النشر، وتأكد أن الضيف يرى `publicCatalog` ولا يقرأ `videosPrivate`، وأن دخول المدير يعمل.

## المدفوعات اليدوية

لا يوجد Paymob أو بوابة دفع أو تأكيد تلقائي أو أي ادعاء بأن Visa/PayPal/المحافظ مجانية. يرسل المستخدم اسم الوسيلة، مزودها/محفظتها، رقم العملية، ورقم الهاتف أو بريد المرسل إلى طلب ينتظر المراجعة؛ يرى المسؤول الأدلة ويقبل الطلب يدوياً من لوحة VIP. أدخل **بيانات الاستلام الخاصة بك** في إعدادات تعليمات الدفع. لا تطلب من المستخدم رمز OTP أو رقم PIN أو بيانات البطاقة، ولا تعتبر لقطة الشاشة وحدها دليلاً على وصول الأموال.

## تغيير رابط صفحة الإدارة

يمكنك إعادة تسمية `public/admin.html`، لكن الاسم المشفر ليس حاجز صلاحيات. إذا غيّرت الاسم، حدّث مساري rewrite في `vercel.json`، ولا تغيّر اعتماد الحماية على تسجيل الدخول والـ custom claim وقواعد Firebase.
