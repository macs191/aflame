import React, { useEffect, useState } from 'react';
import { collection, getDocs } from 'firebase/firestore';
import { db } from '@/lib/firebase';
import { Series as SeriesType, Episode } from '@/types';
import { VideoPlayer } from '@/components/player/VideoPlayer';
import { Clapperboard, Star, Calendar, Loader2, Play, Layers, X, Tv } from 'lucide-react';

export const Series: React.FC = () => {
  const [seriesList, setSeriesList] = useState<SeriesType[]>([]); const [selectedSeries, setSelectedSeries] = useState<SeriesType | null>(null); const [episodes, setEpisodes] = useState<Episode[]>([]); const [activeEpisode, setActiveEpisode] = useState<Episode | null>(null); const [loading, setLoading] = useState(true); const [loadingEpisodes, setLoadingEpisodes] = useState(false);
  useEffect(() => { const fetchSeries = async () => { try { const snap = await getDocs(collection(db, 'series')); setSeriesList(snap.docs.map((d) => ({ id: d.id, ...d.data() })) as SeriesType[]); } catch (err) { console.error('فشل في جلب المسلسلات:', err); } finally { setLoading(false); } }; void fetchSeries(); }, []);
  const handleSelectSeries = async (series: SeriesType) => { setSelectedSeries(series); setLoadingEpisodes(true); setActiveEpisode(null); try { const snap = await getDocs(collection(db, `series/${series.id}/episodes`)); const list = snap.docs.map((d) => ({ id: d.id, ...d.data() })) as Episode[]; list.sort((a, b) => a.episodeNumber - b.episodeNumber); setEpisodes(list); if (list.length) setActiveEpisode(list[0]); } catch (err) { console.error('فشل في جلب حلقات المسلسل:', err); } finally { setLoadingEpisodes(false); } };
  if (loading) return <div className="min-h-screen bg-dark-900 flex items-center justify-center text-gold-500"><Loader2 className="w-10 h-10 animate-spin" /></div>;
  return <div className="min-h-screen bg-dark-900 text-white p-6 md:p-10"><div className="max-w-7xl mx-auto space-y-8"><div className="flex items-center justify-between border-b border-gold-500/20 pb-4"><h1 className="text-3xl font-extrabold text-gold-500 flex items-center gap-3"><Clapperboard className="w-8 h-8" /> مكتبة المسلسلات</h1><span className="text-sm text-gray-400">إجمالي المسلسلات: {seriesList.length}</span></div>{selectedSeries && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/95 p-4 md:p-8 backdrop-blur-md overflow-y-auto"><div className="relative w-full max-w-6xl bg-dark-800 rounded-2xl p-6 border border-gold-500/30 space-y-6 my-auto"><button onClick={() => { setSelectedSeries(null); setActiveEpisode(null); }} className="absolute top-4 left-4 p-2 text-gray-400 hover:text-white bg-dark-900/80 rounded-full transition-colors z-10"><X className="w-6 h-6" /></button>{activeEpisode && activeEpisode.streamSources?.length > 0 ? <div className="space-y-3"><VideoPlayer source={activeEpisode.streamSources[0]} sources={activeEpisode.streamSources} posterUrl={activeEpisode.thumbnailUrl || selectedSeries.bannerUrl} /><div className="flex items-center justify-between"><h3 className="text-xl font-bold text-gold-400">الحلقة {activeEpisode.episodeNumber}: {activeEpisode.titleAr}</h3></div></div> : <div className="aspect-video bg-dark-900 rounded-2xl flex items-center justify-center text-gray-400">{loadingEpisodes ? <Loader2 className="w-10 h-10 animate-spin text-gold-500" /> : 'اختر حلقة للبدء في المشاهدة'}</div>}<div className="grid grid-cols-1 md:grid-cols-3 gap-6 pt-4 border-t border-gold-500/10"><div className="md:col-span-1 space-y-3"><h2 className="text-2xl font-bold text-white">{selectedSeries.titleAr}</h2><p className="text-gray-400 text-sm leading-relaxed">{selectedSeries.descriptionAr}</p><div className="flex items-center gap-4 text-xs text-gray-400 pt-2"><span className="flex items-center gap-1 text-gold-500"><Star className="w-4 h-4 fill-gold-500" /> {selectedSeries.rating}</span><span className="flex items-center gap-1"><Layers className="w-4 h-4" /> {selectedSeries.totalSeasons} مواسم</span></div></div><div className="md:col-span-2 space-y-3"><h4 className="text-lg font-bold text-gold-400 flex items-center gap-2"><Tv className="w-5 h-5" /> قائمة الحلقات</h4>{loadingEpisodes ? <div className="py-8 text-center text-gray-400">جاري تحميل الحلقات...</div> : episodes.length > 0 ? <div className="grid grid-cols-2 sm:grid-cols-3 gap-3 max-h-60 overflow-y-auto p-1">{episodes.map((ep) => <button key={ep.id} onClick={() => setActiveEpisode(ep)} className={`p-3 rounded-xl border text-right transition-all flex flex-col justify-between ${activeEpisode?.id === ep.id ? 'border-gold-500 bg-gold-500/20 text-gold-300 font-bold' : 'border-gold-500/10 bg-dark-900/50 hover:border-gold-500/30 text-gray-300'}`}><span className="text-xs text-gold-500 mb-1">حلقة {ep.episodeNumber}</span><span className="text-sm line-clamp-1">{ep.titleAr}</span></button>)}</div> : <div className="py-8 text-center text-gray-400">لا توجد حلقات مضافة حالياً لهذا المسلسل.</div>}</div></div></div></div>}{seriesList.length > 0 ? <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-6">{seriesList.map((series) => <div key={series.id} onClick={() => void handleSelectSeries(series)} className="group cursor-pointer glass-card glass-card-hover rounded-2xl overflow-hidden flex flex-col transition-all duration-300"><div className="relative aspect-[2/3] overflow-hidden bg-dark-800"><img src={series.posterUrl} alt={series.titleAr} loading="lazy" className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" /><div className="absolute inset-0 bg-gradient-to-t from-dark-900 via-transparent to-transparent opacity-80" /><div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity bg-black/40"><div className="p-4 bg-gold-500 text-dark-900 rounded-full shadow-lg shadow-gold-500/30"><Play className="w-8 h-8 fill-dark-900" /></div></div><div className="absolute top-3 right-3 bg-dark-900/80 backdrop-blur-md text-gold-400 text-xs font-bold px-2.5 py-1 rounded-lg border border-gold-500/20 flex items-center gap-1"><Star className="w-3 h-3 fill-gold-400" /> {series.rating}</div></div><div className="p-4 flex flex-col flex-1 justify-between"><h3 className="font-bold text-lg text-white group-hover:text-gold-400 transition-colors line-clamp-1">{series.titleAr}</h3><div className="flex items-center justify-between text-xs text-gray-400 mt-2"><span className="flex items-center gap-1"><Calendar className="w-3.5 h-3.5" /> {series.releaseYear}</span><span className="flex items-center gap-1"><Layers className="w-3.5 h-3.5" /> {series.totalSeasons} مواسم</span></div></div></div>)}</div> : <div className="glass-card p-12 text-center rounded-2xl text-gray-400">لا توجد مسلسلات متاحة في المكتبة حالياً.</div>}</div></div>;
};
export default Series;
